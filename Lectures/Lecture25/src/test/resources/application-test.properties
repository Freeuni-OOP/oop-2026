# ??? Test profile ? overrides application.properties ??????
# Tests use H2 in-memory so Docker is NOT required to run them.
# This file is activated automatically for all tests via @ActiveProfiles("test")
# or by the spring.test.properties mechanism.

spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

