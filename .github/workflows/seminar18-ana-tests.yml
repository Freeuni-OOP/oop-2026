name: Test seminar 18
on:
  pull_request:
    paths:
      - "Seminars/Ana/18/**"

jobs:
  install:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "8"
          cache: maven

      - name: Run maven clean install
        run: |
          cd Seminars/Ana/18
          mvn -B clean install -DskipTests

  test:
    runs-on: ubuntu-latest
    needs: install

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "8"
          cache: maven

      - name: Run Maven test
        run: |
          cd Seminars/Ana/18
          mvn -B test

    
          
          



