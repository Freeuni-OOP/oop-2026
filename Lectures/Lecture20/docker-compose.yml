services:

  mysql:
    image: mysql:8
    container_name: mysql_db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: student_db
    ports:
      - "3307:3307"

  web:
    build: .
    container_name: java_web
    ports:
      - "8080:8080"
    depends_on:
      - mysql