services:
  mysql:
    image: mysql:8.4
    container_name: lecture25-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: lecture25db
      MYSQL_USER: lecture25user
      MYSQL_PASSWORD: lecture25pass
      MYSQL_ROOT_PASSWORD: rootpass
    ports:
      - "3307:3306"          # host:container — connect on localhost:3307 (3306 may be taken by a local MySQL)
    volumes:
      - mysql_data:/var/lib/mysql   # persist data between restarts
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "lecture25user", "-plecture25pass"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mysql_data:
